class Point {
  constructor(who_served, who_won, point_number) {
    this.who_served = who_served; // 1 or 2
    this.who_won = who_won; // 1 or 2
    this.point_number = point_number;
  }
}

export default Point;